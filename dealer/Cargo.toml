[package]
name = "dealer"
version = "0.1.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
black_jack_client = { path= "../client"}
utils = { path="../utils"}
std-semaphore="0.1.0"
crossbeam-channel="0.5.5"